define(function () {

    return {
        tileSize: 3,      // tile size for cells in CanvasView
        padding: 1,       // tile padding for CanvasView
        stepDuration: 20, // milliseconds  between Board Generations
        defaultSize: 100  // the size of the _board (columns / rows)
    };

});